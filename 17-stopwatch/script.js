const timeDisplay=document.getElementById('time'),startBtn=document.getElementById('start'),stopBtn=document.getElementById('stop'),resetBtn=document.getElementById('reset');let interval,seconds=0,minutes=0,hours=0;function updateDisplay(){timeDisplay.textContent=`${String(hours).padStart(2,'0')}:${String(minutes).padStart(2,'0')}:${String(seconds).padStart(2,'0')}`}function start(){clearInterval(interval);interval=setInterval(()=>{seconds++;if(seconds===60){seconds=0;minutes++;if(minutes===60){minutes=0;hours++}}updateDisplay()},1000)}function stop(){clearInterval(interval)}function reset(){clearInterval(interval);seconds=0;minutes=0;hours=0;updateDisplay()}startBtn.onclick=start;stopBtn.onclick=stop;resetBtn.onclick=reset;
