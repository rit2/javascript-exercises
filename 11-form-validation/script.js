const form=document.getElementById('form'),name=document.getElementById('name'),email=document.getElementById('email'),password=document.getElementById('password'),success=document.getElementById('success');form.onsubmit=e=>{e.preventDefault();let valid=true;document.querySelectorAll('.error').forEach(el=>el.textContent='');document.querySelectorAll('input').forEach(el=>el.classList.remove('invalid'));if(name.value.trim().length<3){document.getElementById('nameError').textContent='Name must be 3+ characters';name.classList.add('invalid');valid=false}if(!/\S+@\S+\.\S+/.test(email.value)){document.getElementById('emailError').textContent='Invalid email';email.classList.add('invalid');valid=false}if(password.value.length<6){document.getElementById('passwordError').textContent='Password must be 6+ characters';password.classList.add('invalid');valid=false}if(valid){success.classList.add('show');setTimeout(()=>success.classList.remove('show'),3000);form.reset()}};
