const form=document.getElementById('form'),input=document.getElementById('input'),list=document.getElementById('list'),count=document.getElementById('count'),update=()=>count.textContent=list.children.length;form.onsubmit=e=>{e.preventDefault();const text=input.value.trim();if(!text)return;const li=document.createElement('li'),span=document.createElement('span'),btn=document.createElement('button');span.textContent=text;btn.textContent='Delete';btn.className='delete';btn.onclick=()=>{li.remove();update()};li.append(span,btn);list.appendChild(li);input.value='';update()};
