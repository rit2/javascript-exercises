const minutesInput=document.getElementById('minutes'),secondsInput=document.getElementById('seconds'),startBtn=document.getElementById('start'),resetBtn=document.getElementById('reset'),timeDisplay=document.getElementById('time');let interval,totalSeconds;function updateDisplay(){const m=Math.floor(totalSeconds/60),s=totalSeconds%60;timeDisplay.textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}function start(){clearInterval(interval);totalSeconds=parseInt(minutesInput.value)*60+parseInt(secondsInput.value);if(totalSeconds<=0)return;updateDisplay();interval=setInterval(()=>{totalSeconds--;updateDisplay();if(totalSeconds<=0){clearInterval(interval);alert('Time is up!')}},1000)}function reset(){clearInterval(interval);minutesInput.value=1;secondsInput.value=0;timeDisplay.textContent='00:00'}startBtn.onclick=start;resetBtn.onclick=reset;
